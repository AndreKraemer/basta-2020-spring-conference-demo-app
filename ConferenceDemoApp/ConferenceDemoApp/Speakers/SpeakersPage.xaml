<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ConferenceDemoApp.Speakers.SpeakersPage">
    <ContentPage.Content>
        <CollectionView Margin="20" 
                        ItemsSource="{Binding Speakers}"
                        SelectionMode="Single"
                        SelectionChanged="SpeakerSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <Label Text="{Binding Name}" FontAttributes="Bold"></Label>
                        <Label Text="{Binding Company}" FontAttributes="Italic" VerticalOptions="End"></Label>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>